# iotta Configuration
# Copy this file to .env and fill in your values.

# Admin token – full access to all endpoints and devices
# Generate with: openssl rand -hex 32
IOTTA_API_TOKEN=changeme


# Encryption key – used to encrypt credentials and group tokens in the DB
# Generate with: openssl rand -hex 32
IOTTA_SECRET_KEY=changeme
